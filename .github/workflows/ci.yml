name: CI

on:
  push:
    branches: [main, master]
    paths-ignore:
      - 'Casks/**'
      - '*.md'
  pull_request:
    branches: [main, master]

jobs:
  build:
    runs-on: macos-15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build
        run: make build

      - name: Create Bundle
        run: make bundle
